doctype html
html
    head
        script(src ="https://ajax.googleapis.com/ajax/libs/jquery/3.2.0/jquery.min.js")
    body
        h1 HOME
        form(method="get" action="/logout" id="logoutForm")
             input(type="submit" value="Logout")
        p User:
        span #{user}
        hr
        p My lists
        hr
        form(method="get", action="/listManagement" id="listManagementForm")
            input(type="submit" value="Create list" name="createList")
            input(type="submit" value="Create list" name="createList")